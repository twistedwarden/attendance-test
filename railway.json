{
  "build": {
    "services": {
      "attendance-monolith": {
        "rootDirectory": "backend",
        "buildCommand": "npm ci --omit=dev && cd .. && npm ci --include=dev && npm run build && cd backend",
        "startCommand": "node server.js"
      }
    }
  }
} 